<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.noe.qrcode" >
  <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_add_courseFragment"
            android:label="Add Course">

        </activity>
        <activity android:name=".add_instructor_fragment"
            android:label="Add Instructor">

        </activity>
        <activity
            android:name=".addStudent"
            android:label="@string/add_student" >
        </activity>
        <activity
            android:name=".startClass"
            android:label="@string/title_start_class" >
        </activity>
        <activity android:name=".enrollActivity"
            android:label="Enroll Student">

        </activity>
    </application>

</manifest>
